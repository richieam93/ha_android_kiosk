[project]
name = "ha-android-kiosk"
version = "1.9.12"
description = "Home Assistant custom integration and Android kiosk app"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = []
